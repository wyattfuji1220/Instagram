<!doctype html>
<html lang="ja">
<head>
<meta charset="utf-8">
<style>
  :root {
    /* 文字の倍率。カルーセルは1、リールは9:16で小さく見えるため大きくする。 */
    --s: {{ scale | default(1) }};
    /* 補助的な文字は本文ほど大きくしない。同じ倍率だと横一行に収まらず
       折り返してしまう（↑詳細レビューは…が2行になった）。 */
    --s-sm: {{ small_scale | default(1) }};
    --ink: #FFFFFF;
    --ink-dim: #D8D4CC;
    --hl: #F2C14E;          /* 本文のハイライト（黄〜橙） */
    --hl-cover: #F05545;    /* 表紙のハイライト（赤） */
  }

  * { margin: 0; padding: 0; box-sizing: border-box; }

  html, body {
    width: {{ width }}px;
    height: {{ height }}px;
    overflow: hidden;
    background: #0A0A0A;
    color: var(--ink);
    font-family: "Noto Serif JP", "Noto Serif CJK JP", "Hiragino Mincho ProN",
                 "Yu Mincho", "YuMincho", serif;
    -webkit-font-smoothing: antialiased;
  }

  .card {
    position: relative;
    width: {{ width }}px;
    height: {{ height }}px;
    display: flex;
    flex-direction: column;
  }

  /* 背景はぼかして沈める。文字の可読性を最優先にする。 */
  .bg {
    position: absolute;
    inset: -40px;
    background-image: url("{{ bg }}");
    background-size: cover;
    background-position: center;
    filter: blur(4px) brightness(0.78) saturate(0.9);
  }
  .veil {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse at center,
        rgba(6, 6, 8, 0.26) 0%, rgba(6, 6, 8, 0.60) 100%),
      linear-gradient(180deg,
        rgba(6, 6, 8, 0.62) 0%, rgba(6, 6, 8, 0.36) 42%, rgba(6, 6, 8, 0.70) 100%);
  }

  .layer {
    position: relative;
    flex: 1;
    display: flex;
    flex-direction: column;
    padding: {{ pad_top | default(68) }}px 76px {{ pad_bottom | default(62) }}px;
  }

  .top-note {
    font-size: calc(31px * var(--s-sm));
    letter-spacing: 0.04em;
    color: var(--ink-dim);
    white-space: nowrap;
  }

  .center {
    flex: 1;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    text-align: center;
  }

  .lead {
    font-size: calc(58px * var(--s));
    font-weight: 500;
    line-height: 1.95;
    letter-spacing: 0.02em;
    white-space: pre-line;
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.6);
    max-width: 900px;
  }
  .lead.small { font-size: calc(50px * var(--s)); line-height: 2.0; }
  .hl { color: var(--hl); }

  .foot {
    display: flex;
    align-items: center;
    justify-content: center;
    gap: calc(20px * var(--s));
  }
  .icon {
    width: calc(78px * var(--s)); height: calc(78px * var(--s)); border-radius: 50%;
    background-image: url("{{ icon }}");
    background-size: cover; background-position: center;
    background-color: #2A2A2A;
    flex: 0 0 auto;
  }
  .icon.letter {
    display: flex; align-items: center; justify-content: center;
    font-size: calc(36px * var(--s)); color: var(--ink-dim);
  }
  .handle { font-size: calc(40px * var(--s-sm)); letter-spacing: 0.02em; }

  /* ------------------------------------------------------------------ 表紙 */
  /* グリッド(4:5)で切られないよう、要素をすべて中央に寄せる。 */

  .cover-frame {
    position: absolute;
    inset: 44px;
    border: 2px solid rgba(255, 255, 255, 0.5);
    pointer-events: none;
  }
  .cover-tagline {
    text-align: center;
    font-size: calc(34px * var(--s-sm));
    letter-spacing: 0.12em;
    color: var(--ink-dim);
  }
  .cover-title {
    font-size: calc(82px * var(--s));
    font-weight: 700;
    line-height: 1.68;
    letter-spacing: 0.01em;
    white-space: pre-line;
    text-align: center;
    text-shadow: 0 4px 26px rgba(0, 0, 0, 0.75);
  }
  .cover-title .hl { color: var(--hl-cover); }
  .cover-tag {
    font-size: calc(34px * var(--s));
    letter-spacing: 0.08em;
    color: var(--ink-dim);
    margin-bottom: 32px;
    text-align: center;
  }

  /* -------------------------------------------------------------- 書誌情報 */

  .biblio-title {
    margin-top: 40px;
    font-size: calc(48px * var(--s));
    font-weight: 600;
    text-align: center;
    line-height: 1.5;
    text-wrap: balance;
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.6);
  }
  .biblio-body {
    flex: 1;
    display: flex;
    align-items: center;
    gap: calc(54px * var(--s));
    padding: 44px 0;
  }
  .cover-image {
    width: 420px; height: 600px;
    background-image: url("{{ cover_image }}");
    background-size: cover; background-position: center;
    background-color: #16161A;
    box-shadow: 0 18px 48px rgba(0, 0, 0, 0.6);
    flex: 0 0 auto;
  }
  .cover-image.placeholder {
    display: flex; align-items: center; justify-content: center;
    text-align: center; padding: 40px;
    font-size: calc(40px * var(--s)); line-height: 1.7; color: var(--ink-dim);
    border: 1px solid #3A3A40;
  }
  .facts { flex: 1; }
  .fact { margin-bottom: 52px; }
  .fact-label { font-size: calc(38px * var(--s)); color: var(--hl); margin-bottom: 16px; }
  .fact-value {
    font-size: calc(42px * var(--s));
    line-height: 1.6;
    /* 訳者まで入る長い著者名は 9:16 の拡大時に右へ溢れる。折り返す。 */
    overflow-wrap: anywhere;
    word-break: normal;
  }
  .biblio-note { font-size: calc(31px * var(--s-sm)); text-align: center; color: var(--ink-dim); }

  /* ---------------------------------------------------- こんな方におすすめ */

  .rec-title {
    margin-top: 24px;
    font-size: calc(56px * var(--s));
    letter-spacing: 0.05em;
    margin-bottom: 64px;
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.6);
  }
  .rec-list { width: 100%; max-width: 900px; }
  .rec-item {
    display: flex; align-items: flex-start; gap: calc(28px * var(--s));
    margin-bottom: 52px; text-align: left;
  }
  .check {
    flex: 0 0 auto; width: 48px; height: 48px;
    border: 3px solid var(--ink);
    display: flex; align-items: center; justify-content: center;
    font-size: calc(34px * var(--s)); line-height: 1; margin-top: 10px;
  }
  .rec-text {
    font-size: calc(44px * var(--s)); line-height: 1.75;
    /* 折り返し位置は Python 側で文節を見て決め、改行文字で渡す。
       ブラウザ任せだと語の途中で切れ、「方」の1文字だけが次行に残る。 */
    white-space: pre-line;
    text-shadow: 0 2px 14px rgba(0, 0, 0, 0.55);
  }

  /* ------------------------------------------------------------ 最終カード */

  .outro-head {
    white-space: pre-line;
    font-size: calc(48px * var(--s));
    line-height: 1.8;
    max-width: 860px;
    text-shadow: 0 2px 18px rgba(0, 0, 0, 0.6);
  }
  .outro-text {
    margin-top: 64px;
    font-size: calc(38px * var(--s));
    line-height: 1.9;
    color: var(--ink-dim);
    white-space: pre-line;
    max-width: 860px;
  }
</style>
</head>
<body>

<div class="card">
  <div class="bg"></div>
  <div class="veil"></div>

  {% if variant == 'cover' %}
    <div class="cover-frame"></div>
    <div class="layer">
      <div class="cover-tagline">{{ top_note }}</div>
      <div class="center">
        <div class="cover-title" style="font-size: {{ font_size }}px">{{ text_html }}</div>
      </div>
      <div class="cover-tag">{{ cover_tag }}</div>
      <div class="foot">
        <div class="icon{{ ' letter' if not icon }}">{{ '' if icon else 'A' }}</div>
        <span class="handle">{{ handle }}</span>
      </div>
    </div>

  {% else %}
    <div class="layer">
      <div class="top-note">{{ top_note }}</div>

      {% if variant == 'biblio' %}
        <div class="biblio-title">{{ book_title }}</div>
        <div class="biblio-body">
          <div class="cover-image{{ '' if cover_image else ' placeholder' }}">
            {{ '' if cover_image else book_title }}
          </div>
          <div class="facts">
            <div class="fact">
              <div class="fact-label">▶ 発行日</div>
              <div class="fact-value">{{ published or '—' }}</div>
            </div>
            <div class="fact">
              <div class="fact-label">▶ 著者</div>
              <div class="fact-value">{{ book_author }}</div>
            </div>
          </div>
        </div>
        <div class="biblio-note">↓ 詳細レビューは本文から ↓</div>

      {% elif variant == 'recommend' %}
        <div class="center">
          <div class="rec-title">こんな方におすすめ</div>
          <div class="rec-list">
            {% for item in items %}
              <div class="rec-item">
                <span class="check">✓</span>
                <span class="rec-text">{{ item }}</span>
              </div>
            {% endfor %}
          </div>
        </div>
        <div class="foot">
          <div class="icon{{ ' letter' if not icon }}">{{ '' if icon else 'A' }}</div>
          <span class="handle">{{ handle }}</span>
        </div>

      {% elif variant == 'outro' %}
        <div class="center">
          <div class="outro-head">{{ account_name }}</div>
          <div class="outro-text">{{ outro_text }}</div>
        </div>
        <div class="foot">
          <div class="icon{{ ' letter' if not icon }}">{{ '' if icon else 'A' }}</div>
          <span class="handle">{{ handle }}</span>
        </div>

      {% else %}
        <div class="center">
          <div class="lead" style="font-size: {{ font_size }}px">{{ text_html }}</div>
        </div>
        <div class="foot">
          <div class="icon{{ ' letter' if not icon }}">{{ '' if icon else 'A' }}</div>
          <span class="handle">{{ handle }}</span>
        </div>
      {% endif %}
    </div>
  {% endif %}
</div>

</body>
</html>
